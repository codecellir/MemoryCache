@model IEnumerable<string>
@{
    ViewData["Title"] = "Home Page";
}

<div class="container">
    <div class="row">
        @foreach (var item in Model)
        {
            <p>@item</p>
        }
    </div>
</div>
